# GT Neo2 Tint Camera

A clean Android camera app rebuilt for realme GT Neo2 (RMX3370).

The app is based on an open-source Camera2/OpenGL camera architecture and adds a real Tint control from -100 to +100 to the preview and recording pipeline.
